{
  "version": "1.0.0",
  "name": "WebApplication1",
  "private": true,
  "dependencies": {
    "bower": "latest",
    "rimraf": "latest",
    "gulp": "latest",
    "gulp-newer": "latest",
		"require-dir": "^0.3.0"
  },
  "scripts": {
    "postinstall": "bower install; gulp build"
  }
}
